# Place a copy of this config to ~/.aerospace.toml
# After that, you can edit ~/.aerospace.toml to your liking

# 'start-at-login' needs to be 'true' for 'after-login-command' to work
# Available commands: https://nikitabobko.github.io/AeroSpace/commands
after-login-command = []

# You can use it to add commands that run after AeroSpace startup.
# 'after-startup-command' is run after 'after-login-command'
# Available commands : https://nikitabobko.github.io/AeroSpace/commands

after-startup-command = [
  'exec-and-forget borders active_color=0x66ffffff order=above hidpi="on" inactive_color=0x00ffffff width=1 style="round"',
  'exec-and-forget sketchybar'
]

# Start AeroSpace at login
start-at-login = true

# Normalizations. See: https://nikitabobko.github.io/AeroSpace/guide#normalization
enable-normalization-flatten-containers = true
enable-normalization-opposite-orientation-for-nested-containers = true

# See: https://nikitabobko.github.io/AeroSpace/guide#layouts
# The 'accordion-padding' specifies the size of accordion padding
# You can set 0 to disable the padding feature
accordion-padding = 30

# Possible values: tiles|accordion
default-root-container-layout = 'tiles'

# Possible values: horizontal|vertical|auto
# 'auto' means: wide monitor (anything wider than high) gets horizontal orientation,
#               tall monitor (anything higher than wide) gets vertical orientation
default-root-container-orientation = 'auto'

# See https://nikitabobko.github.io/AeroSpace/guide#on-focus-changed-callbacks
# See https://nikitabobko.github.io/AeroSpace/commands#move-mouse
# on-focused-monitor-changed = ['move-mouse monitor-lazy-center']

# You can effectively turn off macOS "Hide application" (cmd-h) feature by toggling this flag
# Useful if you don't use this macOS feature, but accidentally hit cmd-h or cmd-alt-h key
# Also see: https://nikitabobko.github.io/AeroSpace/goodies#disable-hide-app
automatically-unhide-macos-hidden-apps = true

# Possible values: (qwerty|dvorak|colemak)
# See https://nikitabobko.github.io/AeroSpace/guide#key-mapping
[key-mapping]
  preset = 'qwerty'

# Gaps between windows (inner-*) and between monitor edges (outer-*).
[gaps]
  inner.horizontal = 10
  inner.vertical =   10
  outer.left =       0
  outer.bottom =     5
  outer.top =        25
  outer.right =      0

# [workspace-to-monitor-force-assignment]
# B = [ 1, 'Built-in Retina Display' ]
# D = [ 2, 'Built-in Retina Display' ]
# E = [ 2, 'Built-in Retina Display' ]
# M = [ 2, 'Built-in Retina Display' ]
# N = [ 1, 'Built-in Retina Display' ]
# O = [ 3, 'Built-in Retina Display' ]
# P = [ 2, 'Built-in Retina Display' ]
# R = [ 3, 'Built-in Retina Display' ]
# S = [ 3, 'Built-in Retina Display' ]
# T = [ 1, 'Built-in Retina Display' ]
# V = [ 3, 'Built-in Retina Display' ]
# W = [ 3, 'Built-in Retina Display' ]

[[on-window-detected]]
if.app-name-regex-substring = 'Finder'
run = 'layout floating'

# Browser to workspace B
[[on-window-detected]]
if.app-name-regex-substring = 'Zen'
run = ['move-node-to-workspace B']

[[on-window-detected]]
if.app-name-regex-substring = 'Firefox'
run = ['move-node-to-workspace B']

# Meetings to workspace M
[[on-window-detected]]
if.app-name-regex-substring = 'Microsoft Teams'
run = ['layout floating', 'move-node-to-workspace M']

# OneNote to workspace N
[[on-window-detected]]
if.app-name-regex-substring = 'Microsoft OneNote'
run = ['layout floating', 'move-node-to-workspace N']

# Slack to workspace S
[[on-window-detected]]
if.app-name-regex-substring = 'Slack'
run = ['move-node-to-workspace S']

# Windows RDP to workspace W
[[on-window-detected]]
if.app-name-regex-substring = 'Windows App'
run = ['move-node-to-workspace W']

# 'main' binding mode declaration
# See: https://nikitabobko.github.io/AeroSpace/guide#binding-modes
[mode.main.binding]
    # See: https://nikitabobko.github.io/AeroSpace/commands#layout
    alt-slash = 'layout tiles horizontal vertical'
    alt-comma = 'layout accordion horizontal vertical'

    # See: https://nikitabobko.github.io/AeroSpace/commands#focus
    alt-h = 'focus left'
    alt-j = 'focus down'
    alt-k = 'focus up'
    alt-l = 'focus right'

    alt-shift-h = 'move-workspace-to-monitor left'
    alt-shift-j = 'move-workspace-to-monitor down'
    alt-shift-k = 'move-workspace-to-monitor up'
    alt-shift-l = 'move-workspace-to-monitor right'

    # See: https://nikitabobko.github.io/AeroSpace/commands#resize
    alt-minus = 'resize smart -50'
    alt-equal = 'resize smart +50'

    # See: https://nikitabobko.github.io/AeroSpace/commands#workspace
    alt-b = ['exec-and-forget open /Applications/Zen.app', 'workspace B'] # Browser workspace
    alt-e = ['exec-and-forget open /Applications/Microsoft\ Excel.app', 'workspace E'] # Excel Workspace
    alt-d = ['exec-and-forget open /Applications/Ghostty.app', 'workspace D'] # Ghostty devoplment neovim workspace
    alt-m = ['exec-and-forget open /Applications/Microsoft\ Teams.app', 'workspace M'] # Teams Meetings workspace
    alt-n = ['exec-and-forget open /Applications/Microsoft\ OneNote.app', 'workspace N'] # OneNote notes workspace
    alt-o = ['exec-and-forget open /Applications/Microsoft\ Outlook.app', 'workspace O'] # Outlook workspace
    alt-p = ['exec-and-forget open /Applications/Microsoft\ Word.app', 'workspace P' ] # Word workspace
    alt-r = ['exec-and-forget open /Applications/Spotify.app', 'workspace R'] # Radio (Spotify)
    alt-s = ['exec-and-forget open /Applications/Slack.app', 'workspace S'] # Slack workspace
    alt-t = ['exec-and-forget open /Applications/iTerm.app', 'workspace T'] # Terminal
    alt-v = ['exec-and-forget open /Applications/Visual\ Studio\ Code.app', 'workspace V'] # VSCode workspace
    alt-w = ['workspace W'] # Randmom workspace
    alt-f = [ 'fullscreen', 'mode main' ]

    # See: https://nikitabobko.github.io/AeroSpace/commands#move-node-to-workspace
    alt-shift-b = 'move-node-to-workspace B'
    alt-shift-e = 'move-node-to-workspace E'
    alt-shift-d = 'move-node-to-workspace D'
    alt-shift-m = 'move-node-to-workspace M'
    alt-shift-n = 'move-node-to-workspace N'
    alt-shift-o = 'move-node-to-workspace O'
    alt-shift-p = 'move-node-to-workspace P'
    alt-shift-r = 'move-node-to-workspace R'
    alt-shift-s = 'move-node-to-workspace S'
    alt-shift-t = 'move-node-to-workspace T'
    alt-shift-v = 'move-node-to-workspace V'
    alt-shift-w = 'move-node-to-workspace W'

    # See: https://nikitabobko.github.io/AeroSpace/commands#workspace-back-and-forth
    alt-tab = 'workspace-back-and-forth'
    # See: https://nikitabobko.github.io/AeroSpace/commands#move-workspace-to-monitor
    alt-shift-tab = 'move-workspace-to-monitor --wrap-around next'
    # See: https://nikitabobko.github.io/AeroSpace/commands#mode
    alt-shift-semicolon = 'mode service'

# 'service' binding mode declaration.
# See: https://nikitabobko.github.io/AeroSpace/guide#binding-modes
[mode.service.binding]
    esc = ['reload-config', 'mode main']
    r = ['flatten-workspace-tree', 'mode main'] # reset layout
    f = ['layout floating tiling', 'mode main'] # Toggle between floating and tiling layout
    backspace = ['close-all-windows-but-current', 'mode main']

    # sticky is not yet supported https://github.com/nikitabobko/AeroSpace/issues/2
    #s = ['layout sticky tiling', 'mode main']

    # alt-shift-h = ['join-with left', 'mode main']
    # alt-shift-j = ['join-with down', 'mode main']
    # alt-shift-k = ['join-with up', 'mode main']
    # alt-shift-l = ['join-with right', 'mode main']

    down = 'volume down'
    up = 'volume up'
    shift-down = ['volume set 0', 'mode main']
